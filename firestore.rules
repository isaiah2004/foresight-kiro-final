service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Allowing all reads and writes for development
    // This removes the need for Firebase authentication while keeping data functionality
    // TODO: Implement proper security rules based on your chosen authentication method
    
    // User and financial data - currently allowing all operations for development
    match /{document=**} {
      allow read, write: if true;
    }
  }
}